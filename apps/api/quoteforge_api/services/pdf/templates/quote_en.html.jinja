<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <style>{{ styles|safe }}</style>
  <style>:root { --accent: {{ contractor.primary_color }}; }</style>
</head>
<body>
  <div class="header">
    <div>
      {% if contractor.logo_url %}<img class="logo" src="{{ contractor.logo_url }}" alt="logo">{% endif %}
      <div class="business">{{ contractor.business_name }}</div>
      <div class="subtle">{{ contractor.contact_name }} &middot; {{ contractor.email }}{% if contractor.phone %} &middot; {{ contractor.phone }}{% endif %}</div>
      {% if contractor.address %}<div class="subtle">{{ contractor.address }}</div>{% endif %}
      {% if contractor.licences %}<div class="licences">{{ contractor.licences|join(" &nbsp; ") }}</div>{% endif %}
    </div>
    <div class="meta">
      <div class="quote-title">Quote</div>
      <div><strong>{{ quote_number }}</strong></div>
      <div>Date: {{ issue_date }}</div>
      <div>Valid until: {{ valid_until }}</div>
    </div>
  </div>

  <div class="parties">
    <div class="party">
      <h3>Prepared for</h3>
      <div><strong>{{ customer.name }}</strong></div>
      {% if customer.company %}<div>{{ customer.company }}</div>{% endif %}
      <div>{{ customer.address_line1 }}</div>
      {% if customer.address_line2 %}<div>{{ customer.address_line2 }}</div>{% endif %}
      <div>{{ customer.city }}, {{ customer.province }} {{ customer.postal_code }}</div>
      {% if customer.email %}<div>{{ customer.email }}</div>{% endif %}
      {% if customer.phone %}<div>{{ customer.phone }}</div>{% endif %}
    </div>
    <div class="party">
      <h3>Job</h3>
      <div><strong>{{ job_title }}</strong></div>
      {% if job_site_address %}<div>Site: {{ job_site_address }}</div>{% endif %}
    </div>
  </div>

  {% if scope %}
  <div class="scope">
    <h3>Scope of work</h3>
    <div>{{ scope }}</div>
  </div>
  {% endif %}

  <table>
    <thead>
      <tr><th>Description</th><th class="num">Qty</th><th class="num">Amount</th></tr>
    </thead>
    <tbody>
      {% for group in groups %}
      <tr class="group-row"><td colspan="3">{{ group.label }}</td></tr>
      {% for item in group.lines %}
      <tr>
        <td>{{ item.description }}</td>
        <td class="num">{{ item.quantity }}</td>
        <td class="num">{{ item.total }}</td>
      </tr>
      {% endfor %}
      {% endfor %}
    </tbody>
  </table>

  <table class="totals">
    <tr><td class="label">Subtotal</td><td class="num">{{ subtotal }}</td></tr>
    {% for tax in tax_lines %}
    <tr><td class="label">{{ tax.label }} ({{ tax.rate }})</td><td class="num">{{ tax.amount }}</td></tr>
    {% endfor %}
    <tr class="grand"><td>Total (CAD)</td><td class="num">{{ total }}</td></tr>
  </table>

  <div class="terms">
    <h3>Terms &amp; conditions</h3>
    <div>{{ terms }}</div>
  </div>

  <div class="signature">
    <div class="sig-line">Customer signature</div>
    <div class="sig-line">Date</div>
  </div>

  <div class="footer-note">Generated by QuoteForge</div>
</body>
</html>
